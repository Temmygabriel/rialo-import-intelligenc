# Import Intelligence MVP uses local reference data by default.
NEXT_PUBLIC_DEMO_MODE=true
# No Rialo secrets are required. Rialo is isolated behind a mock adapter until APIs are verified.
RIALO_ADAPTER=mock
